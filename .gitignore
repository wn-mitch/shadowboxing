# Rust / Cargo
/target/
/rust_analyzer/
# Note: Cargo.lock is intentionally NOT ignored — this is a binary crate and
# committing the lockfile ensures reproducible builds.

# Bevy assets compiled cache
assets/**/*.meta

# IDE
.idea/
.vscode/
*.iml

# macOS
.DS_Store

# Bevy dynamic linking (dev feature)
*.dylib
*.so
*.dll
